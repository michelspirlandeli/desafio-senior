#server:
  #error:
    #include-stacktrace: on_param

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bdsenior?useSSL=false&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update